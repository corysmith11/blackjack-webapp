<h3>Welcome to the Blackjack table <%= session[:name] %>!</h3>

</br>

Dealer's Cards:
<ul>
  <% session[:dealer_cards].each do |card| %>
    <li><%= card.to_s %></li>
  <% end %>
</ul>

</br>

<%= session[:name] %>'s Cards:
<ul>
  <% session[:player_cards].each do |card| %>
    <li><%= card.to_s %></li>
  <% end %>
</ul>
</br>

<p>
What would you like to do?
</br>
<%= session[:name] %> has <%= calc_total(session[:player_cards]) %>

<% if @show_hit_or_stay_buttons %>
<form action='game/player/hit' method='post'>
  <input type="submit" class="btn btn-primary" value="Hit">
</form>
<form action='game/player/stay' method="post">
  <input type="submit" class="btn btn-primary" value="Stay">
</form>
<% end %>
</p>
